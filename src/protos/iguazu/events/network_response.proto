syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Network ErrResponseor not currently in snowflake. creating protobuff for chronosphere
message NetworkResponse {
  // Derived from NETWORK_RESPONSE.APP_ENV
  google.protobuf.StringValue app_env = 1;

  // Derived from NETWORK_RESPONSE.APP_NAME
  google.protobuf.StringValue app_name = 2;

  // Derived from NETWORK_RESPONSE.APP_TYPE
  google.protobuf.StringValue app_type = 3;

  // Derived from NETWORK_RESPONSE.APP_VERSION
  google.protobuf.StringValue app_version = 4;

  // Derived from NETWORK_RESPONSE.APP_WEB_NEXT
  google.protobuf.StringValue app_web_next = 5;

  // Derived from NETWORK_RESPONSE.APP_WEB_SHA
  google.protobuf.StringValue app_web_sha = 6;

  // Derived from NETWORK_RESPONSE.BROWSER_HEIGHT
  google.protobuf.Int64Value browser_height = 7;

  // Derived from NETWORK_RESPONSE.BROWSER_WIDTH
  google.protobuf.Int64Value browser_width = 8;

  // Derived from NETWORK_RESPONSE.CONNECTION_SPEED
  google.protobuf.Int64Value connection_speed = 9;

  // Derived from NETWORK_RESPONSE.CONSUMER_ID
  google.protobuf.Int64Value consumer_id = 10;

  // Derived from NETWORK_RESPONSE.COUNTRY_CODE
  google.protobuf.StringValue country_code = 11;

  // Derived from NETWORK_RESPONSE.DD_DEVICE_ID
  google.protobuf.StringValue dd_device_id = 12;

  // Derived from NETWORK_RESPONSE.DD_DISTRICT_ID
  google.protobuf.StringValue dd_district_id = 13;

  // Derived from NETWORK_RESPONSE.DD_GUEST_ID
  google.protobuf.StringValue dd_guest_id = 14;

  // Derived from NETWORK_RESPONSE.DD_LANGUAGE
  google.protobuf.StringValue dd_language = 15;

  // Derived from NETWORK_RESPONSE.DD_LOCALE
  google.protobuf.StringValue dd_locale = 16;

  // Derived from NETWORK_RESPONSE.DD_LOGIN_ID
  google.protobuf.StringValue dd_login_id = 17;

  // Derived from NETWORK_RESPONSE.DD_SESSION_ID
  google.protobuf.StringValue dd_session_id = 18;

  // Derived from NETWORK_RESPONSE.DD_SUBMARKET_ID
  google.protobuf.StringValue dd_submarket_id = 19;

  // Derived from NETWORK_RESPONSE.DD_ZIP_CODE
  google.protobuf.StringValue dd_zip_code = 20;

  // Derived from NETWORK_RESPONSE.TARGET_APP
  google.protobuf.StringValue target_app = 21;

  // Derived from NETWORK_RESPONSE.PLATFORM
  google.protobuf.StringValue platform = 22;

  // Derived from NETWORK_RESPONSE.PAGE_TYPE
  google.protobuf.StringValue page_type = 23;

  // Derived from NETWORK_RESPONSE.PAGE_ID
  google.protobuf.StringValue page_id = 24;

  // Derived from NETWORK_RESPONSE.URL
  google.protobuf.StringValue url = 25;

  // Derived from NETWORK_RESPONSE.OPERATION_NAME
  google.protobuf.StringValue operation_name = 26;

  // Derived from NETWORK_RESPONSE.METHOD
  google.protobuf.StringValue method = 27;

  // Derived from NETWORK_RESPONSE.RESPONSE_STATUS
  google.protobuf.StringValue response_status = 28;

  // Derived from NETWORK_RESPONSE.RESPONSE_TIME
  google.protobuf.StringValue response_time = 29;

  // Derived from NETWORK_RESPONSE.CORRELATION_ID
  google.protobuf.StringValue correlation_id = 30;
}
