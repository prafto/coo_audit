syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table ITEM_RECOMMENDATION_SHEET
message ItemRecommendationSheet {
  // Derived from ITEM_RECOMMENDATION_SHEET.DD_DEVICE_ID
  google.protobuf.StringValue dd_device_id = 1;

  // Derived from ITEM_RECOMMENDATION_SHEET.COUNTRY_CODE
  google.protobuf.StringValue country_code = 2;

  // Derived from ITEM_RECOMMENDATION_SHEET.COMPONENT
  google.protobuf.StringValue component = 3;

  // Derived from ITEM_RECOMMENDATION_SHEET.APP_VERSION
  google.protobuf.StringValue app_version = 4;

  // Derived from ITEM_RECOMMENDATION_SHEET.DD_PLATFORM
  google.protobuf.StringValue dd_platform = 5;

  // Derived from ITEM_RECOMMENDATION_SHEET.DD_DELIVERY_CORRELATION_ID
  google.protobuf.StringValue dd_delivery_correlation_id = 6;

  // Derived from ITEM_RECOMMENDATION_SHEET.LOCALE
  google.protobuf.StringValue locale = 7;

  // Derived from ITEM_RECOMMENDATION_SHEET.PAGE_ID
  google.protobuf.StringValue page_id = 8;

  // Derived from ITEM_RECOMMENDATION_SHEET.DD_SUBMARKET_ID
  google.protobuf.StringValue dd_submarket_id = 9;

  // Derived from ITEM_RECOMMENDATION_SHEET.IS_GUEST
  google.protobuf.StringValue is_guest = 10;

  // Derived from ITEM_RECOMMENDATION_SHEET.DD_SESSION_ID
  google.protobuf.StringValue dd_session_id = 11;

  // Derived from ITEM_RECOMMENDATION_SHEET.CONSUMER_ID
  google.protobuf.StringValue consumer_id = 12;

  // Derived from ITEM_RECOMMENDATION_SHEET.DD_LOGIN_ID
  google.protobuf.StringValue dd_login_id = 13;

  // Derived from ITEM_RECOMMENDATION_SHEET.TARGET_APP
  google.protobuf.StringValue target_app = 14;

  // Derived from ITEM_RECOMMENDATION_SHEET.EXPERIENCE
  google.protobuf.StringValue experience = 15;

  // --- event specific properties ---

  // Derived from ITEM_RECOMMENDATION_SHEET.EVENT_TYPE
  google.protobuf.StringValue event_type = 16;

  // Derived from ITEM_RECOMMENDATION_SHEET.SHEET_TYPE
  google.protobuf.StringValue sheet_type = 17;

  // Derived from ITEM_RECOMMENDATION_SHEET.SHEET_TITLE
  google.protobuf.StringValue sheet_title = 18;

  // Derived from ITEM_RECOMMENDATION_SHEET.SHEET_SUBTITLE
  google.protobuf.StringValue sheet_subtitle = 19;

  // Derived from ITEM_RECOMMENDATION_SHEET.CART_SUBTOTAL
  google.protobuf.Int64Value cart_subtotal = 20;

  // Derived from ITEM_RECOMMENDATION_SHEET.PROMOTION_SUBTOTAL_REQUIREMENT
  google.protobuf.Int64Value promotion_subtotal_requirement = 21;

  // Derived from ITEM_RECOMMENDATION_SHEET.PLACEMENT
  google.protobuf.StringValue placement = 22;

  // Derived from ITEM_RECOMMENDATION_SHEET.RECOMMENDED_ITEM_IDS
  google.protobuf.StringValue recommended_item_ids = 23;

  // Derived from ITEM_RECOMMENDATION_SHEET.PROMOTION_CAMPAIGN_ID
  google.protobuf.StringValue promotion_campaign_id = 24;

  // Derived from ITEM_RECOMMENDATION_SHEET.PROMOTION_INCENTIVE
  google.protobuf.Int64Value promotion_incentive = 25;

  // Derived from ITEM_RECOMMENDATION_SHEET.PROMOTION_TYPE
  google.protobuf.StringValue promotion_type = 26;

  // Derived from ITEM_RECOMMENDATION_SHEET.CART_ID
  google.protobuf.StringValue cart_id = 27;

  // Derived from ITEM_RECOMMENDATION_SHEET.CART_ITEM_IDS
  google.protobuf.StringValue cart_item_ids = 28;

  // Derived from ITEM_RECOMMENDATION_SHEET.CART_ITEMS_COUNT
  google.protobuf.Int32Value cart_items_count = 29;

  // Derived from ITEM_RECOMMENDATION_SHEET.STORE_ID
  google.protobuf.StringValue store_id = 30;

  // Derived from ITEM_RECOMMENDATION_SHEET.BUSINESS_ID
  google.protobuf.StringValue business_id = 31;

  // Derived from ITEM_RECOMMENDATION_SHEET.SHOULD_DISPLAY
  google.protobuf.BoolValue should_display = 32;

  // Derived from ITEM_RECOMMENDATION_SHEET.NOT_DISPLAY_REASON
  google.protobuf.StringValue not_display_reason = 33;
}
