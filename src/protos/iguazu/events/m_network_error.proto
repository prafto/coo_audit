syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_NETWORK_ERROR
message MNetworkError {
  // Derived from M_NETWORK_ERROR.APP_VERSION
  google.protobuf.StringValue app_version = 1;

  // Derived from M_NETWORK_ERROR.CDN
  google.protobuf.StringValue cdn = 2;

  // Derived from M_NETWORK_ERROR.CLIENT_REQUEST_ID
  google.protobuf.StringValue client_request_id = 3;

  // Derived from M_NETWORK_ERROR.CODE
  google.protobuf.Int64Value code = 4;

  // Derived from M_NETWORK_ERROR.COMPONENT
  google.protobuf.StringValue component = 5;

  // Derived from M_NETWORK_ERROR.CONSUMER_ID
  google.protobuf.StringValue consumer_id = 6;

  // Derived from M_NETWORK_ERROR.CONTEXT
  google.protobuf.StringValue context = 7;

  // Derived from M_NETWORK_ERROR.CORRELATION_ID
  google.protobuf.StringValue correlation_id = 8;

  // Derived from M_NETWORK_ERROR.COUNTRY_CODE
  google.protobuf.StringValue country_code = 9;

  // Derived from M_NETWORK_ERROR.DD_DELIVERY_CORRELATION_ID
  google.protobuf.StringValue dd_delivery_correlation_id = 10;

  // Derived from M_NETWORK_ERROR.DD_DEVICE_ID
  google.protobuf.StringValue dd_device_id = 11;

  // Derived from M_NETWORK_ERROR.DD_DISTRICT_ID
  google.protobuf.Int64Value dd_district_id = 12;

  // Derived from M_NETWORK_ERROR.DD_DISTRICT_IF
  google.protobuf.Int64Value dd_district_if = 13;

  // Derived from M_NETWORK_ERROR.DD_IOS_IDFA_ID
  google.protobuf.StringValue dd_ios_idfa_id = 14;

  // Derived from M_NETWORK_ERROR.DD_IOS_IDFV_ID
  google.protobuf.StringValue dd_ios_idfv_id = 15;

  // Derived from M_NETWORK_ERROR.DD_LOGIN_ID
  google.protobuf.StringValue dd_login_id = 16;

  // Derived from M_NETWORK_ERROR.DD_PLATFORM
  google.protobuf.StringValue dd_platform = 17;

  // Derived from M_NETWORK_ERROR.DD_SESSION_ID
  google.protobuf.StringValue dd_session_id = 18;

  // Derived from M_NETWORK_ERROR.DD_SUBMARKET_ID
  google.protobuf.Int64Value dd_submarket_id = 19;

  // Derived from M_NETWORK_ERROR.DD_USER_ID
  google.protobuf.Int64Value dd_user_id = 20;

  // Derived from M_NETWORK_ERROR.DD_ZIP_CODE
  google.protobuf.StringValue dd_zip_code = 21;

  // Derived from M_NETWORK_ERROR.DOMAIN
  google.protobuf.StringValue domain = 22;

  // Derived from M_NETWORK_ERROR.DOORDASH_CANARY_ALWAYS
  google.protobuf.StringValue doordash_canary_always = 23;

  // Derived from M_NETWORK_ERROR.ENDPOINT_TEMPLATE
  google.protobuf.StringValue endpoint_template = 24;

  // Derived from M_NETWORK_ERROR.ERROR_MESSAGE
  google.protobuf.StringValue error_message = 25;

  // Derived from M_NETWORK_ERROR.EVENT_TEXT
  google.protobuf.StringValue event_text = 26;

  // Derived from M_NETWORK_ERROR.EXPERIENCE
  google.protobuf.StringValue experience = 27;

  // Derived from M_NETWORK_ERROR.IS_GUEST
  google.protobuf.StringValue is_guest = 28;

  // Derived from M_NETWORK_ERROR.IS_REWRITE
  google.protobuf.StringValue is_rewrite = 29;

  // Derived from M_NETWORK_ERROR.LOAD_TIME
  google.protobuf.StringValue load_time = 30;

  // Derived from M_NETWORK_ERROR.LOCALE
  google.protobuf.StringValue locale = 31;

  // Derived from M_NETWORK_ERROR.LOCALIZED_DESCRIPTION
  google.protobuf.StringValue localized_description = 32;

  // Derived from M_NETWORK_ERROR.METHOD
  google.protobuf.StringValue method = 33;

  // Derived from M_NETWORK_ERROR.PAGE_ID
  google.protobuf.StringValue page_id = 34;

  // Derived from M_NETWORK_ERROR.PAGE_TYPE
  google.protobuf.StringValue page_type = 35;

  // Derived from M_NETWORK_ERROR.REQUEST_ID
  google.protobuf.StringValue request_id = 36;

  // Derived from M_NETWORK_ERROR.RESPONSE_BODY
  google.protobuf.StringValue response_body = 37;

  // Derived from M_NETWORK_ERROR.SEGMENT_DEDUPE_ID
  google.protobuf.StringValue segment_dedupe_id = 38;

  // Derived from M_NETWORK_ERROR.SESSION_ID
  google.protobuf.StringValue session_id = 39;

  // Derived from M_NETWORK_ERROR.STATUS_CODE
  google.protobuf.Int64Value status_code = 40;

  // Derived from M_NETWORK_ERROR.TARGET_APP
  google.protobuf.StringValue target_app = 41;

  // Derived from M_NETWORK_ERROR.TRACKER_DATA_KEY1
  google.protobuf.StringValue tracker_data_key1 = 42;

  // Derived from M_NETWORK_ERROR.TRACKER_DATA_KEY2
  google.protobuf.StringValue tracker_data_key2 = 43;

  // Derived from M_NETWORK_ERROR.URL
  google.protobuf.StringValue url = 44;

  // Derived from M_NETWORK_ERROR.UUID_TS
  google.protobuf.Timestamp uuid_ts = 45;

  // Derived from M_NETWORK_ERROR.WEBP
  google.protobuf.StringValue webp = 46;

  // Derived from M_NETWORK_ERROR.X_CLIENT_REQUEST_ID
  google.protobuf.StringValue x_client_request_id = 47;

  // Derived from M_NETWORK_ERROR.X_CORRELATION_ID
  google.protobuf.StringValue x_correlation_id = 48;

  // Derived from M_NETWORK_ERROR.X_SESSION_ID
  google.protobuf.StringValue x_session_id = 49;

  // Derived from M_NETWORK_ERROR.Y_8_N_ID_0_J_92790_G_W_TD_H
  google.protobuf.StringValue y_8_n_id_0_j_92790_g_w_td_h = 50;

  // Derived from M_NETWORK_ERROR.PLATFORM
  google.protobuf.StringValue platform = 51;

  // Derived from M_NETWORK_ERROR.shortened_url
  google.protobuf.StringValue shortened_url = 52;
}
