syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_SIGN_IN_FAILURE
message MSignInFailure {
  // Derived from M_SIGN_IN_FAILURE.DD_ANDROID_ID
  google.protobuf.StringValue dd_android_id = 1;

  // Derived from M_SIGN_IN_FAILURE.DD_PLATFORM
  google.protobuf.StringValue dd_platform = 2;

  // Derived from M_SIGN_IN_FAILURE.EVENT_TEXT
  google.protobuf.StringValue event_text = 3;

  // Derived from M_SIGN_IN_FAILURE.DD_LOGIN_ID
  google.protobuf.StringValue dd_login_id = 4;

  // Derived from M_SIGN_IN_FAILURE.DD_ANDROID_ADVERTISING_ID
  google.protobuf.StringValue dd_android_advertising_id = 5;

  // Derived from M_SIGN_IN_FAILURE.DD_DEVICE_ID
  google.protobuf.StringValue dd_device_id = 6;

  // Derived from M_SIGN_IN_FAILURE.DD_SESSION_ID
  google.protobuf.StringValue dd_session_id = 7;

  // Derived from M_SIGN_IN_FAILURE.UUID_TS
  google.protobuf.Timestamp uuid_ts = 8;

  // Derived from M_SIGN_IN_FAILURE.REASON
  google.protobuf.StringValue reason = 9;

  // Derived from M_SIGN_IN_FAILURE.DD_DISTRICT_ID
  google.protobuf.StringValue dd_district_id = 10;

  // Derived from M_SIGN_IN_FAILURE.DD_SUBMARKET_ID
  google.protobuf.StringValue dd_submarket_id = 11;

  // Derived from M_SIGN_IN_FAILURE.DD_ZIP_CODE
  google.protobuf.StringValue dd_zip_code = 12;

  // Derived from M_SIGN_IN_FAILURE.DD_USER_ID
  google.protobuf.Int64Value dd_user_id = 13;

  // Derived from M_SIGN_IN_FAILURE.DD_IOS_IDFA_ID
  google.protobuf.StringValue dd_ios_idfa_id = 14;

  // Derived from M_SIGN_IN_FAILURE.DD_IOS_IDFV_ID
  google.protobuf.StringValue dd_ios_idfv_id = 15;

  // Derived from M_SIGN_IN_FAILURE.ERROR
  google.protobuf.StringValue error = 16;

  // Derived from M_SIGN_IN_FAILURE.DD_DISTRICT_IF
  google.protobuf.StringValue dd_district_if = 17;

  // Derived from M_SIGN_IN_FAILURE.SOURCE
  google.protobuf.StringValue source = 18;

  // Derived from M_SIGN_IN_FAILURE.TRACKER_DATA_KEY2
  google.protobuf.StringValue tracker_data_key2 = 19;

  // Derived from M_SIGN_IN_FAILURE.TRACKER_DATA_KEY1
  google.protobuf.StringValue tracker_data_key1 = 20;

  // Derived from M_SIGN_IN_FAILURE.DD_DELIVERY_CORRELATION_ID
  google.protobuf.StringValue dd_delivery_correlation_id = 21;
}
