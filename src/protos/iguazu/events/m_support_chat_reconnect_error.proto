syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_SUPPORT_CHAT_RECONNECT_ERROR
message MSupportChatReconnectError {
  // Derived from M_SUPPORT_CHAT_RECONNECT_ERROR.CHANNEL_URL
  google.protobuf.StringValue channel_url = 1;

  // Derived from M_SUPPORT_CHAT_RECONNECT_ERROR.CLIENT
  google.protobuf.StringValue client = 2;

  // Derived from M_SUPPORT_CHAT_RECONNECT_ERROR.ERROR
  google.protobuf.StringValue error = 3;

  // Derived from M_SUPPORT_CHAT_RECONNECT_ERROR.CHAT_VERSION
  google.protobuf.StringValue chat_version = 4;
}
