syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_DASHPASS_PREVIEW_TOGGLED
message MDashpassPreviewToggled {
  // Derived from M_DASHPASS_PREVIEW_TOGGLED.DD_SUBMARKET_ID
  google.protobuf.StringValue dd_submarket_id = 1;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.APP_VERSION
  google.protobuf.StringValue app_version = 2;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.CONSUMER_ID
  google.protobuf.StringValue consumer_id = 3;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.PAGE
  google.protobuf.StringValue page = 4;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.DD_LOGIN_ID
  google.protobuf.StringValue dd_login_id = 5;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.LOCALE
  google.protobuf.StringValue locale = 6;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.EXPERIENCE
  google.protobuf.StringValue experience = 7;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.DD_DEVICE_ID
  google.protobuf.StringValue dd_device_id = 8;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.DD_USER_ID
  google.protobuf.StringValue dd_user_id = 9;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.COUNTRY_CODE
  google.protobuf.StringValue country_code = 10;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.PAGE_ID
  google.protobuf.StringValue page_id = 11;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.USER_ID
  google.protobuf.StringValue user_id = 12;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.TARGET_APP
  google.protobuf.StringValue target_app = 13;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.DD_PLATFORM
  google.protobuf.StringValue dd_platform = 14;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.DD_SESSION_ID
  google.protobuf.StringValue dd_session_id = 15;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.EVENT_NAME
  google.protobuf.StringValue event_name = 16;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.EVENT_DATE
  google.protobuf.StringValue event_date = 17;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.TOGGLE_VALUE
  google.protobuf.BoolValue toggle_value = 18;

  // Derived from M_DASHPASS_PREVIEW_TOGGLED.STORE_ID
  google.protobuf.StringValue store_id = 19;
}
