syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_AUTO_ORDER_ACTIVE_POLLING_START
message MAutoOrderActivePollingStart {
  // Derived from M_AUTO_ORDER_ACTIVE_POLLING_START.EVENT_TEXT
  google.protobuf.StringValue event_text = 1;

  // Derived from M_AUTO_ORDER_ACTIVE_POLLING_START.UUID_TS
  google.protobuf.Timestamp uuid_ts = 2;

  // Derived from M_AUTO_ORDER_ACTIVE_POLLING_START.DEVICE_ID
  google.protobuf.StringValue device_id = 3;

  // Derived from M_AUTO_ORDER_ACTIVE_POLLING_START.EVENT_DATE
  google.protobuf.Int64Value event_date = 4;

  // Derived from M_AUTO_ORDER_ACTIVE_POLLING_START.EVENT_NAME
  google.protobuf.StringValue event_name = 5;

  // Derived from M_AUTO_ORDER_ACTIVE_POLLING_START.RESULT
  google.protobuf.StringValue result = 6;

  // Derived from M_AUTO_ORDER_ACTIVE_POLLING_START.TARGET_APP
  google.protobuf.StringValue target_app = 7;
}
