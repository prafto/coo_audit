syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_DASHPASS_PREVIEW_CLOSED
message MDashpassPreviewClosed {
  // Derived from M_DASHPASS_PREVIEW_CLOSED.DD_SUBMARKET_ID
  google.protobuf.StringValue dd_submarket_id = 1;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.APP_VERSION
  google.protobuf.StringValue app_version = 2;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.CONSUMER_ID
  google.protobuf.StringValue consumer_id = 3;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.PAGE
  google.protobuf.StringValue page = 4;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.DD_LOGIN_ID
  google.protobuf.StringValue dd_login_id = 5;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.LOCALE
  google.protobuf.StringValue locale = 6;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.EXPERIENCE
  google.protobuf.StringValue experience = 7;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.DD_DEVICE_ID
  google.protobuf.StringValue dd_device_id = 8;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.DD_USER_ID
  google.protobuf.StringValue dd_user_id = 9;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.COUNTRY_CODE
  google.protobuf.StringValue country_code = 10;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.PAGE_ID
  google.protobuf.StringValue page_id = 11;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.USER_ID
  google.protobuf.StringValue user_id = 12;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.TARGET_APP
  google.protobuf.StringValue target_app = 13;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.DD_PLATFORM
  google.protobuf.StringValue dd_platform = 14;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.DD_SESSION_ID
  google.protobuf.StringValue dd_session_id = 15;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.EVENT_NAME
  google.protobuf.StringValue event_name = 16;

  // Derived from M_DASHPASS_PREVIEW_CLOSED.EVENT_DATE
  google.protobuf.StringValue event_date = 17;
}
