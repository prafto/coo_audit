syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_ID_CHECK_TAP_SCAN
message MIdCheckTapScan {
  // Derived from M_ID_CHECK_TAP_SCAN.APP_SESSION_ID
  google.protobuf.StringValue app_session_id = 1;

  // Derived from M_ID_CHECK_TAP_SCAN.APP_VERSION_CODE
  google.protobuf.Int64Value app_version_code = 2;

  // Derived from M_ID_CHECK_TAP_SCAN.APP_VERSION_NAME
  google.protobuf.StringValue app_version_name = 3;

  // Derived from M_ID_CHECK_TAP_SCAN.DELIVERY_ID
  google.protobuf.StringValue delivery_id = 4;

  // Derived from M_ID_CHECK_TAP_SCAN.DELIVERY_UUID
  google.protobuf.StringValue delivery_uuid = 5;

  // Derived from M_ID_CHECK_TAP_SCAN.DEVICE_ID
  google.protobuf.StringValue device_id = 6;

  // Derived from M_ID_CHECK_TAP_SCAN.EVENT_DATE
  google.protobuf.Int64Value event_date = 7;

  // Derived from M_ID_CHECK_TAP_SCAN.EVENT_NAME
  google.protobuf.StringValue event_name = 8;

  // Derived from M_ID_CHECK_TAP_SCAN.EVENT_RESULT
  google.protobuf.StringValue event_result = 9;

  // Derived from M_ID_CHECK_TAP_SCAN.EVENT_TEXT
  google.protobuf.StringValue event_text = 10;

  // Derived from M_ID_CHECK_TAP_SCAN.PLATFORM
  google.protobuf.StringValue platform = 11;

  // Derived from M_ID_CHECK_TAP_SCAN.RESULT
  google.protobuf.StringValue result = 12;

  // Derived from M_ID_CHECK_TAP_SCAN.TARGET_APP
  google.protobuf.StringValue target_app = 13;

  // Derived from M_ID_CHECK_TAP_SCAN.USER_VISIBLE_LOCALE
  google.protobuf.StringValue user_visible_locale = 14;

  // Derived from M_ID_CHECK_TAP_SCAN.UUID_TS
  google.protobuf.Timestamp uuid_ts = 15;
}
