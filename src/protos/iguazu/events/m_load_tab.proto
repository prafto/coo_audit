syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_LOAD_TAB
message MLoadTab {
  // Derived from M_LOAD_TAB.DD_DEVICE_ID
  google.protobuf.StringValue dd_device_id = 1;

  // Derived from M_LOAD_TAB.COUNTRY_CODE
  google.protobuf.StringValue country_code = 2;

  // Derived from M_LOAD_TAB.COMPONENT
  google.protobuf.StringValue component = 3;

  // Derived from M_LOAD_TAB.APP_VERSION
  google.protobuf.StringValue app_version = 4;

  // Derived from M_LOAD_TAB.DD_PLATFORM
  google.protobuf.StringValue dd_platform = 5;

  // Derived from M_LOAD_TAB.DD_DELIVERY_CORRELATION_ID
  google.protobuf.StringValue dd_delivery_correlation_id = 6;

  // Derived from M_LOAD_TAB.LOCALE
  google.protobuf.StringValue locale = 7;

  // Derived from M_LOAD_TAB.PAGE_ID
  google.protobuf.StringValue page_id = 8;

  // Derived from M_LOAD_TAB.DD_IOS_IDFA_ID
  google.protobuf.StringValue dd_ios_idfa_id = 9;

  // Derived from M_LOAD_TAB.DD_SUBMARKET_ID
  google.protobuf.StringValue dd_submarket_id = 10;

  // Derived from M_LOAD_TAB.DD_IOS_IDFV_ID
  google.protobuf.StringValue dd_ios_idfv_id = 11;

  // Derived from M_LOAD_TAB.IS_GUEST
  google.protobuf.StringValue is_guest = 12;

  // Derived from M_LOAD_TAB.DD_SESSION_ID
  google.protobuf.StringValue dd_session_id = 13;

  // Derived from M_LOAD_TAB.CONSUMER_ID
  google.protobuf.StringValue consumer_id = 14;

  // Derived from M_LOAD_TAB.DD_LOGIN_ID
  google.protobuf.StringValue dd_login_id = 15;

  // Derived from M_LOAD_TAB.TARGET_APP
  google.protobuf.StringValue target_app = 16;

  // Derived from M_LOAD_TAB.EXPERIENCE
  google.protobuf.StringValue experience = 17;

  // Derived from M_LOAD_TAB.IS_REWRITE
  google.protobuf.StringValue is_rewrite = 18;

  // --- event specific properties ---

  // Derived from M_LOAD_TAB.TAB_NAME
  google.protobuf.StringValue tab_name = 19;

  // Derived from M_LOAD_TAB.HORIZONTAL_POSITION
  google.protobuf.Int64Value horizontal_position = 20;

  // Derived from M_LOAD_TAB.HUB_VERTICAL_POSITION
  google.protobuf.StringValue hub_vertical_position = 21;
}
