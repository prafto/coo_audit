syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// m_tap_refund_replace_confirm
message MTapRefundReplaceConfirm {
  // Derived from M_TAP_REFUND_REPLACE_CONFIRM.STORE_ID
  google.protobuf.StringValue store_id = 1;
  // Derived from M_TAP_REFUND_REPLACE_CONFIRM.DELIVERY_UUID
  google.protobuf.StringValue delivery_uuid = 2;
  // Derived from M_TAP_REFUND_REPLACE_CONFIRM.ACTION
  google.protobuf.StringValue action = 3;
  // Derived from M_TAP_REFUND_REPLACE_CONFIRM.REFUND_AMOUNT
  google.protobuf.StringValue refund_amount = 4;
  // Derived from M_TAP_REFUND_REPLACE_CONFIRM.NUM_TO_REFUND
  google.protobuf.StringValue num_to_refund = 5;
  // Derived from M_TAP_REFUND_REPLACE_CONFIRM.NUM_TO_REPLACE
  google.protobuf.StringValue num_to_replace = 6;
  // Derived from M_TAP_REFUND_REPLACE_CONFIRM.IS_MARKED_OOS
  google.protobuf.StringValue is_marked_oos = 7;
  // Derived from M_TAP_REFUND_REPLACE_CONFIRM.ORIGIN
  google.protobuf.StringValue origin = 8;
}
