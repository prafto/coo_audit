syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_TAP_DROPPING_OFF_MAP
message MTapDroppingOffMap {
  // Derived from M_TAP_DROPPING_OFF_MAP.UUID_TS
  google.protobuf.Timestamp uuid_ts = 1;

  // Derived from M_TAP_DROPPING_OFF_MAP.EVENT_TEXT
  google.protobuf.StringValue event_text = 2;

  // Derived from M_TAP_DROPPING_OFF_MAP.APP_VERSION_NAME
  google.protobuf.StringValue app_version_name = 3;

  // Derived from M_TAP_DROPPING_OFF_MAP.DELIVERY_ID
  google.protobuf.StringValue delivery_id = 4;

  // Derived from M_TAP_DROPPING_OFF_MAP.APP_VERSION_CODE
  google.protobuf.Int64Value app_version_code = 5;

  // Derived from M_TAP_DROPPING_OFF_MAP.DELIVERY_UUID
  google.protobuf.StringValue delivery_uuid = 6;

  // Derived from M_TAP_DROPPING_OFF_MAP.TARGET_APP
  google.protobuf.StringValue target_app = 7;

  // Derived from M_TAP_DROPPING_OFF_MAP.EVENT_DATE
  google.protobuf.Int64Value event_date = 8;

  // Derived from M_TAP_DROPPING_OFF_MAP.DEVICE_ID
  google.protobuf.StringValue device_id = 9;

  // Derived from M_TAP_DROPPING_OFF_MAP.RESULT
  google.protobuf.StringValue result = 10;

  // Derived from M_TAP_DROPPING_OFF_MAP.EVENT_NAME
  google.protobuf.StringValue event_name = 11;
}
