syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_ORDER_CONFIRMED_EVENT_FAILURE
message MOrderConfirmedEventFailure {
  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.IS_NEW_PAY_MODEL_UI
  google.protobuf.BoolValue is_new_pay_model_ui = 1;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.IS_CASH_ORDER
  google.protobuf.BoolValue is_cash_order = 2;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.DELIVERY_ID
  google.protobuf.StringValue delivery_id = 3;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.APP_VERSION_NAME
  google.protobuf.StringValue app_version_name = 4;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.APP_VERSION_CODE
  google.protobuf.Int64Value app_version_code = 5;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.UUID_TS
  google.protobuf.Timestamp uuid_ts = 6;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.ELAPSED_TIME_SINCE_MODAL_APPEARED
  google.protobuf.Int64Value elapsed_time_since_modal_appeared = 7;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.REASON
  google.protobuf.StringValue reason = 8;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.SHIFT_ID
  google.protobuf.StringValue shift_id = 9;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.DASHER_ID
  google.protobuf.StringValue dasher_id = 10;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.STORE_ID
  google.protobuf.StringValue store_id = 11;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.NOTIFICATION_ACTION
  google.protobuf.StringValue notification_action = 12;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.EVENT_TEXT
  google.protobuf.StringValue event_text = 13;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.APP_TIMESTAMP
  google.protobuf.Int64Value app_timestamp = 14;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.PACKAGE_DELIVERY_COUNT
  google.protobuf.Int64Value package_delivery_count = 15;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.IS_PACKAGE
  google.protobuf.BoolValue is_package = 16;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.PACKAGE_ID
  google.protobuf.StringValue package_id = 17;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.IS_SHOP_AND_DELIVER
  google.protobuf.BoolValue is_shop_and_deliver = 18;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.DELIVERY_UUID
  google.protobuf.StringValue delivery_uuid = 19;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.TARGET_APP
  google.protobuf.StringValue target_app = 20;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.RESULT
  google.protobuf.StringValue result = 21;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.IS_SHADOW_DELIVERY
  google.protobuf.BoolValue is_shadow_delivery = 22;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.EVENT_NAME
  google.protobuf.StringValue event_name = 23;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.EVENT_DATE
  google.protobuf.Int64Value event_date = 24;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.DEVICE_ID
  google.protobuf.StringValue device_id = 25;

  // Derived from M_ORDER_CONFIRMED_EVENT_FAILURE.BADGE_TEXT
  google.protobuf.StringValue badge_text = 26;

  // Add field DD_PLATFORM
  google.protobuf.StringValue dd_platform = 27;

  // platform
  google.protobuf.StringValue platform = 28;
}
