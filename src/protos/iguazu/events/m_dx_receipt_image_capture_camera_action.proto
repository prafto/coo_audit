syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table SEGMENT_EVENTS_RAW.DRIVER_PRODUCTION.M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION
message MDxReceiptImageCaptureCameraAction {
  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.DEVICE_ID
  google.protobuf.StringValue device_id = 1;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.STORE_ID
  google.protobuf.StringValue store_id = 2;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.TARGET_APP
  google.protobuf.StringValue target_app = 3;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.BUSINESS_ID
  google.protobuf.StringValue business_id = 4;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.DELIVERY_UUID
  google.protobuf.StringValue delivery_uuid = 5;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.EVENT_NAME
  google.protobuf.StringValue event_name = 6;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.ACTION
  google.protobuf.StringValue action = 7;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.RESULT
  google.protobuf.StringValue result = 8;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.EVENT_TEXT
  google.protobuf.StringValue event_text = 9;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.DASHER_ID
  google.protobuf.StringValue dasher_id = 10;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.USER_VISIBLE_LOCALE
  google.protobuf.StringValue user_visible_locale = 11;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.UUID_TS
  google.protobuf.Timestamp uuid_ts = 12;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.EVENT_DATE
  google.protobuf.Int64Value event_date = 13;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.EVENT_RESULT
  google.protobuf.StringValue event_result = 14;

  // Derived from M_DX_RECEIPT_IMAGE_CAPTURE_CAMERA_ACTION.PLATFORM
  google.protobuf.StringValue platform = 15;
}
