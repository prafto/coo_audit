syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_APP_EVO_ENTRY_POINT_TOOLTIP
message MAppEvoEntryPointTooltip {
  // Derived from M_APP_EVO_ENTRY_POINT_TOOLTIP.DD_DEVICE_ID
  google.protobuf.StringValue dd_device_id = 1;

  // Derived from M_APP_EVO_ENTRY_POINT_TOOLTIP.CONSUMER_ID
  google.protobuf.StringValue consumer_id = 2;

  // Derived from M_APP_EVO_ENTRY_POINT_TOOLTIP.DD_SESSION_ID
  google.protobuf.StringValue dd_session_id = 3;

  // Derived from M_APP_EVO_ENTRY_POINT_TOOLTIP.DD_LOGIN_ID
  google.protobuf.StringValue dd_login_id = 4;

  // Derived from M_APP_EVO_ENTRY_POINT_TOOLTIP.APP_VERSION
  google.protobuf.StringValue app_version = 5;

  // Derived from M_APP_EVO_ENTRY_POINT_TOOLTIP.TIMESTAMP
  google.protobuf.StringValue timestamp = 6;

  // Derived from M_APP_EVO_ENTRY_POINT_TOOLTIP.DD_PLATFORM
  google.protobuf.StringValue dd_platform = 7;

  // --- event specific properties ---

  // Derived from M_APP_EVO_ENTRY_POINT_TOOLTIP.PAGE
  google.protobuf.StringValue page = 8;

  // Derived from M_APP_EVO_ENTRY_POINT_TOOLTIP.ACTION
  google.protobuf.StringValue action = 9;

  // Derived from M_APP_EVO_ENTRY_POINT_TOOLTIP.TOOLTIP_TEXT
  google.protobuf.StringValue tooltip_text = 10;

  // Derived from M_APP_EVO_ENTRY_POINT_TOOLTIP.TOOLTIP_TYPE
  google.protobuf.StringValue tooltip_type = 11;
}
