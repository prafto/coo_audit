syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_MENU_CATEGORY_PAGE_LOAD
message MMenuCategoryPageLoad {
  // Derived from M_MENU_CATEGORY_PAGE_LOAD.CONSUMER_ID
  google.protobuf.StringValue consumer_id = 1;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.CONTEXT
  google.protobuf.StringValue context = 2;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.DD_ANDROID_ADVERTISING_ID
  google.protobuf.StringValue dd_android_advertising_id = 3;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.DD_ANDROID_ID
  google.protobuf.StringValue dd_android_id = 4;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.DD_DELIVERY_CORRELATION_ID
  google.protobuf.StringValue dd_delivery_correlation_id = 5;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.DD_DEVICE_ID
  google.protobuf.StringValue dd_device_id = 6;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.DD_IOS_IDFA_ID
  google.protobuf.StringValue dd_ios_idfa_id = 7;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.DD_IOS_IDFV_ID
  google.protobuf.StringValue dd_ios_idfv_id = 8;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.DD_LOGIN_ID
  google.protobuf.StringValue dd_login_id = 9;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.DD_PLATFORM
  google.protobuf.StringValue dd_platform = 10;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.DD_SESSION_ID
  google.protobuf.StringValue dd_session_id = 11;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.DD_USER_ID
  google.protobuf.Int64Value dd_user_id = 12;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.DD_ZIP_CODE
  google.protobuf.StringValue dd_zip_code = 13;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.DEVICE_ID
  google.protobuf.StringValue device_id = 14;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.EVENT_DATE
  google.protobuf.Int64Value event_date = 15;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.EVENT_NAME
  google.protobuf.StringValue event_name = 16;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.EVENT_TEXT
  google.protobuf.StringValue event_text = 17;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.LOAD_TIME
  google.protobuf.DoubleValue load_time = 18;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.MENU_CATEGORY_ID
  google.protobuf.StringValue menu_category_id = 19;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.MENU_ID
  google.protobuf.StringValue menu_id = 20;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.RESULT
  google.protobuf.StringValue result = 21;

  // Derived from M_MENU_CATEGORY_PAGE_LOAD.STORE_ID
  google.protobuf.Int64Value store_id = 22;
}
