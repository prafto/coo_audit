syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_ID_CHECK_INVALID_ID_MODAL_TAP_CONFIRM
message MIdCheckInvalidIdModalTapConfirm {
  // Derived from M_ID_CHECK_INVALID_ID_MODAL_TAP_CONFIRM.APP_SESSION_ID
  google.protobuf.StringValue app_session_id = 1;

  // Derived from M_ID_CHECK_INVALID_ID_MODAL_TAP_CONFIRM.DELIVERY_UUID
  google.protobuf.StringValue delivery_uuid = 2;

  // Derived from M_ID_CHECK_INVALID_ID_MODAL_TAP_CONFIRM.EVENT_TEXT
  google.protobuf.StringValue event_text = 3;

  // Derived from M_ID_CHECK_INVALID_ID_MODAL_TAP_CONFIRM.IS_EXPIRED
  google.protobuf.BoolValue is_expired = 4;

  // Derived from M_ID_CHECK_INVALID_ID_MODAL_TAP_CONFIRM.IS_UNDERAGE
  google.protobuf.BoolValue is_underage = 5;

  // Derived from M_ID_CHECK_INVALID_ID_MODAL_TAP_CONFIRM.TARGET_APP
  google.protobuf.StringValue target_app = 6;

  // Derived from M_ID_CHECK_INVALID_ID_MODAL_TAP_CONFIRM.UUID_TS
  google.protobuf.Timestamp uuid_ts = 7;
}
