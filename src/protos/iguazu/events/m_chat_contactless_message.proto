syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_CHAT_CONTACTLESS_MESSAGE
message MChatContactlessMessage {
  // Derived from M_CHAT_CONTACTLESS_MESSAGE.SUCCESS
  google.protobuf.BoolValue success = 1;

  // Derived from M_CHAT_CONTACTLESS_MESSAGE.EVENT_TEXT
  google.protobuf.StringValue event_text = 2;

  // Derived from M_CHAT_CONTACTLESS_MESSAGE.DELIVERY_UUID
  google.protobuf.StringValue delivery_uuid = 3;

  // Derived from M_CHAT_CONTACTLESS_MESSAGE.ERROR
  google.protobuf.StringValue error = 4;

  // Derived from M_CHAT_CONTACTLESS_MESSAGE.UUID_TS
  google.protobuf.Timestamp uuid_ts = 5;

  // Derived from M_CHAT_CONTACTLESS_MESSAGE.CHANNEL_URL
  google.protobuf.StringValue channel_url = 6;

  // Derived from M_CHAT_CONTACTLESS_MESSAGE.TYPE
  google.protobuf.StringValue type = 7;

  // Derived from M_CHAT_CONTACTLESS_MESSAGE.EVENT_DATE
  google.protobuf.Int64Value event_date = 8;

  // Derived from M_CHAT_CONTACTLESS_MESSAGE.DEVICE_ID
  google.protobuf.StringValue device_id = 9;

  // Derived from M_CHAT_CONTACTLESS_MESSAGE.RESULT
  google.protobuf.StringValue result = 10;

  // Derived from M_CHAT_CONTACTLESS_MESSAGE.EVENT_NAME
  google.protobuf.StringValue event_name = 11;

  // Derived from M_CHAT_CONTACTLESS_MESSAGE.TARGET_APP
  google.protobuf.StringValue target_app = 12;

  // Add field DD_PLATFORM
  google.protobuf.StringValue dd_platform = 13;
}
