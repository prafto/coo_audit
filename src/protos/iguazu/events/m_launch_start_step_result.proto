syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_LAUNCH_START_STEP_RESULT
message MLaunchStartStepResult {
  // Derived from M_LAUNCH_START_STEP_RESULT.ERROR_MESSAGE
  google.protobuf.StringValue error_message = 1;

  // Derived from M_LAUNCH_START_STEP_RESULT.EVENT_TEXT
  google.protobuf.StringValue event_text = 2;

  // Derived from M_LAUNCH_START_STEP_RESULT.RESULT
  google.protobuf.StringValue result = 3;

  // Derived from M_LAUNCH_START_STEP_RESULT.UUID_TS
  google.protobuf.Timestamp uuid_ts = 4;

  // Add field M_LAUNCH_START_STEP_RESULT.DD_PLATFORM
  google.protobuf.StringValue dd_platform = 5;

  // Derived from M_LAUNCH_START_STEP_RESULT.APP_VERSION
  google.protobuf.StringValue app_version = 6;

  // Derived from M_LAUNCH_START_STEP_RESULT.EXPERIENCE
  google.protobuf.StringValue experience = 7;

  // Derived from M_LAUNCH_START_STEP_RESULT.TARGET_APP
  google.protobuf.StringValue target_app = 8;

  // Derived from M_LAUNCH_START_STEP_RESULT.IS_GUEST_CONSUMER
  google.protobuf.StringValue is_guest_consumer = 9;

  // Derived from M_LAUNCH_START_STEP_RESULT.EVENTRESULT
  google.protobuf.StringValue event_result = 10;
}
