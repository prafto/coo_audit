syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Fired when a category on the category page loads
message MItemCategoryLoad {
  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.dd_device_id
  google.protobuf.StringValue dd_device_id = 1;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.country_code
  google.protobuf.StringValue country_code = 2;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.app_version
  google.protobuf.StringValue app_version = 3;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.dd_platform
  google.protobuf.StringValue dd_platform = 4;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.user_visible_locale
  google.protobuf.StringValue user_visible_locale = 5;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.component
  google.protobuf.StringValue component = 6;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.dd_delivery_correlation_id
  google.protobuf.StringValue dd_delivery_correlation_id = 7;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.locale
  google.protobuf.StringValue locale = 8;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.page_id
  google.protobuf.StringValue page_id = 9;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.dd_ios_idfa_id
  google.protobuf.StringValue dd_ios_idfa_id = 10;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.dd_ios_idfv_id
  google.protobuf.StringValue dd_ios_idfv_id = 11;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.dd_submarket_id
  google.protobuf.StringValue dd_submarket_id = 13;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.is_guest
  google.protobuf.StringValue is_guest = 14;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.dd_session_id
  google.protobuf.StringValue dd_session_id = 15;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.consumer_id
  google.protobuf.StringValue consumer_id = 16;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.dd_login_id
  google.protobuf.StringValue dd_login_id = 17;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.target_app
  google.protobuf.StringValue target_app = 18;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.experience
  google.protobuf.StringValue experience = 19;

  // Derived from M_LOYALTY_EMPTY_ENTERPRISE_MODULES.is_rewrite
  google.protobuf.StringValue is_rewrite = 20;

  // The item category name
  google.protobuf.StringValue item_category_name = 21;

  // The item category position
  google.protobuf.Int64Value item_category_position = 22;

  // The item category id
  google.protobuf.StringValue item_category_id = 23;

  // The store id
  google.protobuf.StringValue store_id = 24;

  // The business id
  google.protobuf.StringValue business_id = 25;

  // The category id
  google.protobuf.StringValue category_id = 26;

  // The attribution source
  google.protobuf.StringValue attr_src = 27;

  // The page
  google.protobuf.StringValue page = 28;

  // The child store id, if this is a bundling context
  google.protobuf.StringValue o2_store_id = 29;

  // The bundling context
  google.protobuf.StringValue bundling_context = 30;
}
