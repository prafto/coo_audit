syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_OFFERS_HUB_PAGE_VIEW
message MOffersHubPageView {
  // Derived from M_OFFERS_HUB_PAGE_VIEW.DD_SESSION_ID
  google.protobuf.StringValue dd_session_id = 1;

  // Derived from M_OFFERS_HUB_PAGE_VIEW.IS_REWRITE
  google.protobuf.StringValue is_rewrite = 2;

  // Derived from M_OFFERS_HUB_PAGE_VIEW.COMPONENT
  google.protobuf.StringValue component = 3;

  // Derived from M_OFFERS_HUB_PAGE_VIEW.APP_VERSION
  google.protobuf.StringValue app_version = 4;

  // Derived from M_OFFERS_HUB_PAGE_VIEW.DD_LOGIN_ID
  google.protobuf.StringValue dd_login_id = 5;

  // Derived from M_OFFERS_HUB_PAGE_VIEW.IS_GUEST_CONSUMER
  google.protobuf.BoolValue is_guest_consumer = 6;

  // Derived from M_OFFERS_HUB_PAGE_VIEW.ATTR_SRC
  google.protobuf.StringValue attr_src = 7;

  // Derived from M_OFFERS_HUB_PAGE_VIEW.CONSUMER_ID
  google.protobuf.StringValue consumer_id = 8;

  // Derived from M_OFFERS_HUB_PAGE_VIEW.DD_SUBMARKET_ID
  google.protobuf.StringValue dd_submarket_id = 9;

  // Derived from M_OFFERS_HUB_PAGE_VIEW.DD_PLATFORM
  google.protobuf.StringValue dd_platform = 10;

  // Derived from M_OFFERS_HUB_PAGE_VIEW.TARGET_APP
  google.protobuf.StringValue target_app = 11;

  // Derived from M_OFFERS_HUB_PAGE_VIEW.DD_DELIVERY_CORRELATION_ID
  google.protobuf.StringValue dd_delivery_correlation_id = 12;

  // Derived from M_OFFERS_HUB_PAGE_VIEW.COUNTRY_CODE
  google.protobuf.StringValue country_code = 13;
}
