syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// in_app_rating_card_caviar
message MInAppRatingCardCaviar {
  // Derived from IN_APP_RATING_CARD_CAVIAR.PLATFORM
  google.protobuf.StringValue platform = 1;

  // Derived from IN_APP_RATING_CARD_CAVIAR.TARGE_TAPP
  google.protobuf.StringValue target_app = 2;

  // Derived from IN_APP_RATING_CARD_CAVIAR.EVENTRESULT
  google.protobuf.StringValue event_result = 3;

  // Derived from IN_APP_RATING_CARD_CAVIAR.ERRORTYPE
  google.protobuf.StringValue error_type = 4;

  // Derived from IN_APP_RATING_CARD_CAVIAR.ERRORMESSAGE
  google.protobuf.StringValue error_message = 5;
}
