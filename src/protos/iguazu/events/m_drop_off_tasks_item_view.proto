syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table M_DROP_OFF_TASKS_ITEM_VIEW
message MDropOffTasksItemView {
  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.APP_BUILD_TYPE
  google.protobuf.StringValue app_build_type = 1;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.APP_SESSION_ID
  google.protobuf.StringValue app_session_id = 2;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.APP_SESSION_SEGMENT
  google.protobuf.StringValue app_session_segment = 3;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.APP_VERSION_CODE
  google.protobuf.Int64Value app_version_code = 4;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.APP_VERSION_NAME
  google.protobuf.StringValue app_version_name = 5;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.DELIVERY_ID
  google.protobuf.StringValue delivery_id = 6;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.DELIVERY_UUID
  google.protobuf.StringValue delivery_uuid = 7;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.DEVICE_ID
  google.protobuf.StringValue device_id = 8;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.EVENT_DATE
  google.protobuf.Int64Value event_date = 9;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.EVENT_NAME
  google.protobuf.StringValue event_name = 10;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.EVENT_RESULT
  google.protobuf.StringValue event_result = 11;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.EVENT_TEXT
  google.protobuf.StringValue event_text = 12;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.INTEGRATIONS_ADJUST
  google.protobuf.BoolValue integrations_adjust = 13;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.INTEGRATIONS_ALL
  google.protobuf.BoolValue integrations_all = 14;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.INTEGRATIONS_FACEBOOK_CONVERSIONS_API_ACTIONS
  google.protobuf.BoolValue integrations_facebook_conversions_api_actions = 15;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.INTEGRATIONS_GOOGLE_TAG_MANAGER
  google.protobuf.BoolValue integrations_google_tag_manager = 16;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.INTEGRATIONS_ITERABLE
  google.protobuf.BoolValue integrations_iterable = 17;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.INTEGRATIONS_NEW_RELIC
  google.protobuf.BoolValue integrations_new_relic = 18;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.INTEGRATIONS_OPTIMIZELY
  google.protobuf.BoolValue integrations_optimizely = 19;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.INTEGRATIONS_REPEATER
  google.protobuf.BoolValue integrations_repeater = 20;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.INTEGRATIONS_SLACK
  google.protobuf.BoolValue integrations_slack = 21;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.INTEGRATIONS_SNAPCHAT_CONVERSIONS_API
  google.protobuf.BoolValue integrations_snapchat_conversions_api = 22;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.INTEGRATIONS_SNOWFLAKE
  google.protobuf.BoolValue integrations_snowflake = 23;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.INTEGRATIONS_TIK_TOK_CONVERSIONS
  google.protobuf.BoolValue integrations_tik_tok_conversions = 24;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.IS_CONTACTLESS
  google.protobuf.BoolValue is_contactless = 25;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.IS_UNIFIED_DROP_OFF
  google.protobuf.BoolValue is_unified_drop_off = 26;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.PLATFORM
  google.protobuf.StringValue platform = 27;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.RESULT
  google.protobuf.StringValue result = 28;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.SIGNATURE_REQUIRED
  google.protobuf.BoolValue signature_required = 29;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.TARGET_APP
  google.protobuf.StringValue target_app = 30;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.TYPE
  google.protobuf.StringValue type = 31;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.USER_VISIBLE_LOCALE
  google.protobuf.StringValue user_visible_locale = 32;

  // Derived from M_DROP_OFF_TASKS_ITEM_VIEW.UUID_TS
  google.protobuf.Timestamp uuid_ts = 33;
}
