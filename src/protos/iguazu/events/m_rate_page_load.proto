syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table SEGMENT_EVENTS_RAW.CONSUMER_PRODUCTION.M_RATE_PAGE_LOAD
message MRatePageLoad {
  // Derived from M_RATE_PAGE_LOAD.DD_USER_ID
  google.protobuf.Int64Value dd_user_id = 1;

  // Derived from M_RATE_PAGE_LOAD.DD_DEVICE_ID
  google.protobuf.StringValue dd_device_id = 2;

  // Derived from M_RATE_PAGE_LOAD.DD_ZIP_CODE
  google.protobuf.StringValue dd_zip_code = 3;

  // Derived from M_RATE_PAGE_LOAD.EVENT_TEXT
  google.protobuf.StringValue event_text = 4;

  // Derived from M_RATE_PAGE_LOAD.DD_LOGIN_ID
  google.protobuf.StringValue dd_login_id = 5;

  // Derived from M_RATE_PAGE_LOAD.UUID_TS
  google.protobuf.Timestamp uuid_ts = 6;

  // Derived from M_RATE_PAGE_LOAD.DD_IOS_IDFA_ID
  google.protobuf.StringValue dd_ios_idfa_id = 7;

  // Derived from M_RATE_PAGE_LOAD.DD_DISTRICT_ID
  google.protobuf.Int64Value dd_district_id = 8;

  // Derived from M_RATE_PAGE_LOAD.DD_IOS_IDFV_ID
  google.protobuf.StringValue dd_ios_idfv_id = 9;

  // Derived from M_RATE_PAGE_LOAD.DD_SESSION_ID
  google.protobuf.StringValue dd_session_id = 10;

  // Derived from M_RATE_PAGE_LOAD.DD_SUBMARKET_ID
  google.protobuf.Int64Value dd_submarket_id = 11;

  // Derived from M_RATE_PAGE_LOAD.DD_DISTRICT_IF
  google.protobuf.Int64Value dd_district_if = 12;

  // Derived from M_RATE_PAGE_LOAD.SEGMENT_DEDUPE_ID
  google.protobuf.StringValue segment_dedupe_id = 13;

  // Derived from M_RATE_PAGE_LOAD.TRACKER_DATA_KEY2
  google.protobuf.StringValue tracker_data_key2 = 14;

  // Derived from M_RATE_PAGE_LOAD.TRACKER_DATA_KEY1
  google.protobuf.StringValue tracker_data_key1 = 15;

  // Derived from M_RATE_PAGE_LOAD.DOORDASH_CANARY_ALWAYS
  google.protobuf.StringValue doordash_canary_always = 16;

  // Derived from M_RATE_PAGE_LOAD.VERSION
  google.protobuf.Int64Value version = 17;

  // Derived from M_RATE_PAGE_LOAD.DELIVERY_ID
  google.protobuf.StringValue delivery_id = 18;

  // Derived from M_RATE_PAGE_LOAD.DEVICE_ID
  google.protobuf.StringValue device_id = 19;

  // Derived from M_RATE_PAGE_LOAD.EVENT_NAME
  google.protobuf.StringValue event_name = 20;

  // Derived from M_RATE_PAGE_LOAD.TARGET_APP
  google.protobuf.StringValue target_app = 21;

  // Derived from M_RATE_PAGE_LOAD.RESULT
  google.protobuf.StringValue result = 22;

  // Derived from M_RATE_PAGE_LOAD.EVENT_DATE
  google.protobuf.Int64Value event_date = 23;

  // Derived from M_RATE_PAGE_LOAD.ORDER_ID
  google.protobuf.StringValue order_id = 24;

  // Derived from M_RATE_PAGE_LOAD.CONSUMER_ID
  google.protobuf.StringValue consumer_id = 25;

  // Derived from M_RATE_PAGE_LOAD.DELIVERY_UUID
  google.protobuf.StringValue delivery_uuid = 26;

  // Derived from M_RATE_PAGE_LOAD.STORE_ID
  google.protobuf.StringValue store_id = 27;

  // Derived from M_RATE_PAGE_LOAD.DD_ANDROID_ID
  google.protobuf.StringValue dd_android_id = 28;

  // Derived from M_RATE_PAGE_LOAD.DD_ANDROID_ADVERTISING_ID
  google.protobuf.StringValue dd_android_advertising_id = 29;

  // Derived from M_RATE_PAGE_LOAD.DD_PLATFORM
  google.protobuf.StringValue dd_platform = 30;

  // Derived from M_RATE_PAGE_LOAD.TYPE
  google.protobuf.StringValue type = 31;

  // Derived from M_RATE_PAGE_LOAD.SCORE
  google.protobuf.Int64Value score = 32;

  // Derived from M_RATE_PAGE_LOAD.ORDER_UUID
  google.protobuf.StringValue order_uuid = 33;

  // Derived from M_RATE_PAGE_LOAD.DD_DELIVERY_CORRELATION_ID
  google.protobuf.StringValue dd_delivery_correlation_id = 34;

  // Derived from M_RATE_PAGE_LOAD.IS_MERCHANT_SHIPPING
  google.protobuf.BoolValue is_merchant_shipping = 35;

  // Derived from M_RATE_PAGE_LOAD.IS_REWRITE
  google.protobuf.StringValue is_rewrite = 36;

  // Derived from M_RATE_PAGE_LOAD.ERROR_TYPE
  google.protobuf.StringValue error_type = 37;

  // Derived from M_RATE_PAGE_LOAD.ERROR_MESSAGE
  google.protobuf.StringValue error_message = 38;

  // Derived from M_RATE_PAGE_LOAD.COUNTRY_CODE
  google.protobuf.StringValue country_code = 39;

  // Derived from M_RATE_PAGE_LOAD.LOCALE
  google.protobuf.StringValue locale = 40;

  // Derived from M_RATE_PAGE_LOAD.PAGE_TYPE
  google.protobuf.StringValue page_type = 41;

  // Derived from M_RATE_PAGE_LOAD.IS_GUEST
  google.protobuf.StringValue is_guest = 42;

  // Derived from M_RATE_PAGE_LOAD.EXPERIENCE
  google.protobuf.StringValue experience = 43;

  // Derived from M_RATE_PAGE_LOAD.APP_VERSION
  google.protobuf.StringValue app_version = 44;

  // Derived from M_RATE_PAGE_LOAD.PAGE_ID
  google.protobuf.StringValue page_id = 45;

  // Derived from M_RATE_PAGE_LOAD.COMPONENT
  google.protobuf.StringValue component = 46;

  // Derived from M_RATE_PAGE_LOAD.USER_VISIBLE_LOCALE
  google.protobuf.StringValue user_visible_locale = 47;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_TV_SQUARED
  google.protobuf.BoolValue integrations_tv_squared = 48;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_AMAZON_KINESIS_FIREHOSE
  google.protobuf.BoolValue integrations_amazon_kinesis_firehose = 49;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_OPTIMIZELY_WEB
  google.protobuf.BoolValue integrations_optimizely_web = 50;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_TWITTER_ADS
  google.protobuf.BoolValue integrations_twitter_ads = 51;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_FIREBASE
  google.protobuf.BoolValue integrations_firebase = 52;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_IMPACT_PARTNERSHIP_CLOUD
  google.protobuf.BoolValue integrations_impact_partnership_cloud = 53;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_ADJUST
  google.protobuf.BoolValue integrations_adjust = 54;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_ALL
  google.protobuf.BoolValue integrations_all = 55;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_GOOGLE_TAG_MANAGER
  google.protobuf.BoolValue integrations_google_tag_manager = 56;

  // Derived from M_RATE_PAGE_LOAD.IS_GUEST_CONSUMER
  google.protobuf.BoolValue is_guest_consumer = 57;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_OPTIMIZELY
  google.protobuf.BoolValue integrations_optimizely = 58;

  // Derived from M_RATE_PAGE_LOAD.EVENT_RESULT
  google.protobuf.StringValue event_result = 59;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_GOOGLE_ADS_CONVERSIONS
  google.protobuf.BoolValue integrations_google_ads_conversions = 60;

  // Derived from M_RATE_PAGE_LOAD.PLATFORM
  google.protobuf.StringValue platform = 61;

  // Derived from M_RATE_PAGE_LOAD.PHOTO_INCENTIVE_COUNT
  google.protobuf.StringValue photo_incentive_count = 62;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_TIK_TOK_CONVERSIONS
  google.protobuf.BoolValue integrations_tik_tok_conversions = 63;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_SNAPCHAT_CONVERSIONS_API
  google.protobuf.BoolValue integrations_snapchat_conversions_api = 64;

  // Derived from M_RATE_PAGE_LOAD.INTEGRATIONS_FACEBOOK_CONVERSIONS_API_ACTIONS
  google.protobuf.BoolValue integrations_facebook_conversions_api_actions = 65;

  // Derived from M_RATE_PAGE_LOAD.ITEM_STAR_RATING
  google.protobuf.StringValue item_star_rating = 66;

  // Derived from M_RATE_PAGE_LOAD.NUM_CUSTOMER_PHOTOS
  google.protobuf.StringValue num_customer_photos = 67;

  // Derived from M_RATE_PAGE_LOAD.ITEM_MSID
  google.protobuf.StringValue item_msid = 68;

  // Derived from M_RATE_PAGE_LOAD.ITEM_NUM_OF_REVIEWS
  google.protobuf.StringValue item_num_of_reviews = 69;

  // Derived from M_RATE_PAGE_LOAD.ITEM_ID
  google.protobuf.StringValue item_id = 70;

  // Derived from M_RATE_PAGE_LOAD.ITEM_NUM_STAR_RATING
  google.protobuf.StringValue item_num_star_rating = 71;

  // Derived from M_RATE_PAGE_LOAD.ANDROID_APP_SET_ID
  google.protobuf.StringValue android_app_set_id = 72;

  // Derived from M_RATE_PAGE_LOAD.SINGULAR_DEVICE_ID
  google.protobuf.StringValue singular_device_id = 73;

  // Derived from M_RATE_PAGE_LOAD.OPERATING_SYSTEM_VERSION_STRING
  google.protobuf.StringValue operating_system_version_string = 74;
}
