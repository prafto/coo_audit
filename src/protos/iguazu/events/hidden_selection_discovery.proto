syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from Snowflake table HIDDEN_SELECTION_DISCOVERY
message HiddenSelectionDiscovery {
  // Derived from HIDDEN_SELECTION_DISCOVERY.LAT
  google.protobuf.DoubleValue lat = 1;

  // Derived from HIDDEN_SELECTION_DISCOVERY.CONSUMER_ID
  google.protobuf.StringValue consumer_id = 2;

  // Derived from HIDDEN_SELECTION_DISCOVERY.CONSUMER_PROFILE_AVAILABLE
  google.protobuf.BoolValue consumer_profile_available = 3;

  // Derived from HIDDEN_SELECTION_DISCOVERY.DD_DEVICE_ID
  google.protobuf.StringValue dd_device_id = 4;

  // Derived from HIDDEN_SELECTION_DISCOVERY.DD_SESSION_ID
  google.protobuf.StringValue dd_session_id = 5;

  // Derived from HIDDEN_SELECTION_DISCOVERY.EVENT_TEXT
  google.protobuf.StringValue event_text = 6;

  // Derived from HIDDEN_SELECTION_DISCOVERY.UUID_TS
  google.protobuf.Timestamp uuid_ts = 7;

  // Derived from HIDDEN_SELECTION_DISCOVERY.GEOHASH
  google.protobuf.StringValue geohash = 8;

  // Derived from HIDDEN_SELECTION_DISCOVERY.NUM_RESULTS
  google.protobuf.Int64Value num_results = 9;

  // Derived from HIDDEN_SELECTION_DISCOVERY.SEARCH_SERVICE_ENDPOINT
  google.protobuf.StringValue search_service_endpoint = 10;

  // Derived from HIDDEN_SELECTION_DISCOVERY.DISTRICT
  google.protobuf.Int64Value district = 11;

  // Derived from HIDDEN_SELECTION_DISCOVERY.HIDDEN_SELECTION_DISCOVERY_BUCKET
  google.protobuf.StringValue hidden_selection_discovery_bucket = 12;

  // Derived from HIDDEN_SELECTION_DISCOVERY.LNG
  google.protobuf.DoubleValue lng = 13;

  // Derived from HIDDEN_SELECTION_DISCOVERY.FILTERED_STORE_IDS
  google.protobuf.StringValue filtered_store_ids = 14;

  // Derived from HIDDEN_SELECTION_DISCOVERY.LIMIT
  google.protobuf.Int64Value limit = 15;

  // Derived from HIDDEN_SELECTION_DISCOVERY.RETAINED_STORE_IDS
  google.protobuf.StringValue retained_store_ids = 16;

  // Derived from HIDDEN_SELECTION_DISCOVERY.OFFSET
  google.protobuf.Int64Value offset = 17;
}
