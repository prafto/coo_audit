syntax = "proto3";

package iguazu.events.v1;

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/doordash/services-protobuf/generated/iguazu/events";
option java_multiple_files = true;
option java_package = "com.doordash.iguazu.events.v1";

// Derived from the Snowflake table RECURRING_DELIVERY_ITEM_SELECT
message RecurringDeliveryItemSelect {
  // Derived from RECURRING_DELIVERY_ITEM_SELECT.APP_ENV
  google.protobuf.StringValue app_env = 1;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.APP_NAME
  google.protobuf.StringValue app_name = 2;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.APP_TYPE
  google.protobuf.StringValue app_type = 3;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.APP_WEB_SHA
  google.protobuf.StringValue app_web_sha = 4;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.BROWSER_HEIGHT
  google.protobuf.Int64Value browser_height = 5;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.BROWSER_WIDTH
  google.protobuf.Int64Value browser_width = 6;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.BUILD_TYPE
  google.protobuf.StringValue build_type = 7;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.CONNECTION_SPEED
  google.protobuf.Int64Value connection_speed = 8;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.CONSUMER_ID
  google.protobuf.Int64Value consumer_id = 9;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.CORRELATION_EVENT_ID
  google.protobuf.StringValue correlation_event_id = 10;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.COUNTRY_CODE
  google.protobuf.StringValue country_code = 11;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.DD_DEVICE_ID
  google.protobuf.StringValue dd_device_id = 12;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.DD_DISTRICT_ID
  google.protobuf.StringValue dd_district_id = 13;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.DD_LANGUAGE
  google.protobuf.StringValue dd_language = 14;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.DD_LAST_LOGIN_ACTION
  google.protobuf.StringValue dd_last_login_action = 15;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.DD_LAST_LOGIN_METHOD
  google.protobuf.StringValue dd_last_login_method = 16;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.DD_LOGIN_ID
  google.protobuf.StringValue dd_login_id = 17;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.DD_SESSION_ID
  google.protobuf.StringValue dd_session_id = 18;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.DD_SUBMARKET_ID
  google.protobuf.StringValue dd_submarket_id = 19;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.DD_ZIP_CODE
  google.protobuf.StringValue dd_zip_code = 20;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.EVENT_TEXT
  google.protobuf.StringValue event_text = 21;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.EXPERIENCE
  google.protobuf.StringValue experience = 22;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.FBP
  google.protobuf.StringValue fbp = 23;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.HAS_COMPLETED_FIRST_ORDER
  google.protobuf.BoolValue has_completed_first_order = 24;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.HREF
  google.protobuf.StringValue href = 25;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.IS_GUEST
  google.protobuf.BoolValue is_guest = 26;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.IS_SEGMENT_SCRIPT_LOADED
  google.protobuf.BoolValue is_segment_script_loaded = 27;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.IS_SSR
  google.protobuf.BoolValue is_ssr = 28;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.LOCALE
  google.protobuf.StringValue locale = 29;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.PAGE_ID
  google.protobuf.StringValue page_id = 30;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.PAGE_TYPE
  google.protobuf.StringValue page_type = 31;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.PLATFORM
  google.protobuf.StringValue platform = 32;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.REFERRER
  google.protobuf.StringValue referrer = 33;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.TARGET_APP
  google.protobuf.StringValue target_app = 34;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.TOUCH
  google.protobuf.BoolValue touch = 35;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.USER_AGENT
  google.protobuf.StringValue user_agent = 36;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.USING_TELEMETRY_JS
  google.protobuf.BoolValue using_telemetry_js = 37;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.UTM_CAMPAIGN
  google.protobuf.StringValue utm_campaign = 38;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.UTM_MEDIUM
  google.protobuf.StringValue utm_medium = 39;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.UTM_SOURCE
  google.protobuf.StringValue utm_source = 40;

  // Derived from RECURRING_DELIVERY_ITEM_SELECT.UUID_TS
  google.protobuf.Timestamp uuid_ts = 41;
}
