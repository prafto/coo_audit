syntax = "proto3";

package graph_proxy.v1;

option go_package = "./graph_proxy";
option java_generic_services = true;
option java_multiple_files = true;
option java_package = "com.doordash.graph_proxy";

// to debug the graph proxy
service DebuggingService {
  // this is a dummy endpoint to test the graph proxy
  rpc Endpoint1(Endpoint1Request) returns (Endpoint1Response);
  // this is a dummy endpoint to test the graph proxy
  rpc Endpoint2(Endpoint2Request) returns (Endpoint2Response);
  // this is a dummy endpoint to test the graph proxy
  rpc Endpoint3(Endpoint3Request) returns (Endpoint3Response);
}

// to debug the graph proxy
message Endpoint1Request {
  // debug payload
  string payload = 1;
}

// to debug the graph proxy
message Endpoint1Response {
  // debug payload
  string payload = 1;
}

// to debug the graph proxy
message Endpoint2Request {
  // debug payload
  string payload = 1;
}

// to debug the graph proxy
message Endpoint2Response {
  // debug payload
  string payload = 1;
}

// to debug the graph proxy
message Endpoint3Request {
  // debug payload
  string payload = 1;
}

// to debug the graph proxy
message Endpoint3Response {
  // debug payload
  string payload = 1;
}
