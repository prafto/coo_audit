syntax = "proto3";
package risk.internal.v2;

option java_multiple_files = false;
option java_outer_classname = "DummyProtos";
option java_package = "com.doordash.risk.proto.internal";

// It seems we can't have a directory that doesn't have any
// proto files in the first level
// Add this dummy proto to avoid that error
message Dummy {
  // value
  string value = 1;
}
