syntax = "proto3";
package genai_gateway_service.v1;

option java_multiple_files = true;
option java_package = "com.doordash.genai_gateway_service.rpc.api";

// Status codes for upsert requests
enum StatusCode {
  SUCCESS = 0;
  FAILURE = 1;
  // Primarily for upsert actions, where nothing was changed.
  NOOP = 2;
}
