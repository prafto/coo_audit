syntax = "proto3";

package iguazu.test.common.v1;

import "google/protobuf/wrappers.proto";

option java_multiple_files = true;
option java_package = "com.doordash.iguazu.test.common.v1";

// Entity representing Retail Category L1 (aisles)
message CxCategoryL1 {
  // the ID of the L1 category.
  string category_l1_id = 1;
  // the name of the L1 category. Null if we don't know it.
  google.protobuf.StringValue category_l1_name = 2;
  // the ranked position of the L1 category.
  int32 category_l1_position = 3;
  // Test field for string value
  google.protobuf.StringValue test_field = 4;
}
